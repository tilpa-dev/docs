---
title: 'Quickstart'
description: 'Build an adaptive API in record time'
---

Start using the Tilpa Adaptive API SDK for Python by installing `tilpa-api-sdk` through pip:

```sh
pip install tilpa-api-sdk
```

